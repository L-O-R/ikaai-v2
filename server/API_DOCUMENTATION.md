# Public API Documentation

This document describes the public JSON API exposed by the IKAAI INDIA CMS server.

Current API base path:

```text
/api/
```

Example local base URL:

```text
http://127.0.0.1:8000/api/
```

All public endpoints, including Blogs and the new Jobs API, are mounted under the unversioned `/api/` path. The previously documented `/api/v1/` prefix for Blogs has been removed; Blogs now lives at `/api/blogs/`. If versioning is reintroduced later, update `config/urls.py`, tests, and this document together.

## General API Rules

- Public read APIs do not require authentication.
- Public inquiry submission does not require authentication.
- Admin/CMS write operations are handled through Django Admin, not public JSON APIs.
- Public project, statistic, client, update, and blog APIs return active/published records only.
- Request and response bodies use JSON unless the endpoint redirects to Google OAuth.
- Uploaded image fields are returned as URLs or paths generated by Django REST Framework.
- Dates and internal IDs are not exposed by current public serializers.

## Status Codes

| Status Code | Meaning |
| --- | --- |
| `200 OK` | Request succeeded. |
| `201 Created` | Resource was created successfully. |
| `400 Bad Request` | Request JSON failed validation. |
| `404 Not Found` | Requested active object does not exist. |
| `405 Method Not Allowed` | HTTP method is not supported for the endpoint. |
| `500 Internal Server Error` | Unexpected server error. |

## Error Format

Validation errors follow Django REST Framework's default error format.

Example:

```json
{
  "email": [
    "Enter a valid email address."
  ],
  "message": [
    "Message is required."
  ]
}
```

Detail errors may use this shape:

```json
{
  "detail": "Not found."
}
```

## Authentication

The public JSON endpoints listed below do not require API tokens, JWT, or sessions.

Admin authentication is separate and uses Google OAuth with Django sessions:

- `GET /admin/login/`
- `GET /accounts/google/login/`
- `GET /accounts/google/callback/`

These Google routes are browser redirect routes, not JSON APIs.

## Endpoints Summary

| Method | Endpoint | Authentication | Purpose |
| --- | --- | --- | --- |
| `GET` | `/api/projects/` | Public | List active projects. |
| `GET` | `/api/projects/{slug}/` | Public | Retrieve one active project. |
| `POST` | `/api/inquiries/` | Public | Submit an inquiry. |
| `GET` | `/api/statistics/` | Public | List active organization statistics. |
| `GET` | `/api/clients/` | Public | List active clients. |
| `GET` | `/api/updates/` | Public | List active updates/announcements. |
| `GET` | `/api/blogs/` | Public | List published blog posts. |
| `GET` | `/api/blogs/{slug}/` | Public | Retrieve one published blog post. |
| `GET` | `/api/jobs/jobs/` | Public | List active job openings. |
| `GET` | `/api/jobs/jobs/{slug}/` | Public | Retrieve one active job opening. |
| `POST` | `/api/jobs/job-applications/` | Public | Submit a job application. |

## Projects API

Projects represent public website work/case-study items.

Only projects with `is_active=True` are returned.

### List Projects

```http
GET /api/projects/
```

Returns a paginated list of active projects.

### Query Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `page` | integer | No | Page number. |
| `page_size` | integer | No | Number of items per page. Default is `12`, maximum is `48`. |
| `featured` | boolean | No | Filter by featured status. Use `true` or `false`. |
| `client` | string | No | Exact, case-insensitive client name. |
| `location` | string | No | Case-insensitive partial location match. |
| `search` | string | No | Searches project title, client name, and location. |
| `ordering` | string | No | Sort by `created_at`, `display_order`, or `title`. Prefix with `-` for descending order. |

Default ordering:

```text
display_order, title
```

### Example Requests

```http
GET /api/projects/
```

```http
GET /api/projects/?featured=true
```

```http
GET /api/projects/?client=Example%20Client
```

```http
GET /api/projects/?location=Delhi
```

```http
GET /api/projects/?search=school&page=2&page_size=6
```

```http
GET /api/projects/?ordering=-created_at
```

### Success Response

Status:

```text
200 OK
```

Response body:

```json
{
  "count": 25,
  "next": "http://127.0.0.1:8000/api/projects/?page=2",
  "previous": null,
  "results": [
    {
      "slug": "annual-impact-report",
      "title": "Annual Impact Report",
      "cover_image": "/media/projects/cover/report.png",
      "client": "Example Client",
      "client_logo": "/media/clients/logos/example.png",
      "location": "Delhi",
      "featured": true,
      "short_description": "This is a short public summary of the project."
    }
  ]
}
```

### Response Fields

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `count` | integer | No | Total number of matching projects. |
| `next` | string | Yes | URL for the next page. |
| `previous` | string | Yes | URL for the previous page. |
| `results` | array | No | List of project objects. |
| `results[].slug` | string | No | Unique project slug. |
| `results[].title` | string | No | Project title. |
| `results[].cover_image` | string | No | Cover image URL/path. |
| `results[].client` | string | No | Client name. |
| `results[].client_logo` | string | Yes | Client logo URL/path, or `null` if unavailable. |
| `results[].location` | string | No | Project location. May be an empty string. |
| `results[].featured` | boolean | No | Whether the project is featured. |
| `results[].short_description` | string | No | Description shortened to 160 characters. |

### Retrieve Project Detail

```http
GET /api/projects/{slug}/
```

Returns one active project by slug.

### Path Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `slug` | string | Yes | Project slug. |

### Example Request

```http
GET /api/projects/annual-impact-report/
```

### Success Response

Status:

```text
200 OK
```

Response body:

```json
{
  "slug": "annual-impact-report",
  "title": "Annual Impact Report",
  "cover_image": "/media/projects/cover/report.png",
  "client": "Example Client",
  "client_logo": "/media/clients/logos/example.png",
  "description": "Full project description shown on the project detail page.",
  "location": "Delhi",
  "featured": true,
  "statistics": [
    {
      "title": "People Reached",
      "value": "12000",
      "material_symbol": "groups"
    }
  ],
  "gallery": [
    {
      "image": "/media/projects/gallery/project-image.png",
      "caption": "Community workshop"
    }
  ]
}
```

### Response Fields

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `slug` | string | No | Unique project slug. |
| `title` | string | No | Project title. |
| `cover_image` | string | No | Cover image URL/path. |
| `client` | string | No | Client name. |
| `client_logo` | string | Yes | Client logo URL/path, or `null` if unavailable. |
| `description` | string | No | Full project description. May be an empty string. |
| `location` | string | No | Project location. May be an empty string. |
| `featured` | boolean | No | Whether the project is featured. |
| `statistics` | array | No | Project-specific statistics ordered by `display_order`. |
| `statistics[].title` | string | No | Statistic label. |
| `statistics[].value` | string | No | Statistic value as text. |
| `statistics[].material_symbol` | string | No | Material Symbols icon name. |
| `gallery` | array | No | Project gallery images ordered by `display_order`. |
| `gallery[].image` | string | No | Gallery image URL/path. |
| `gallery[].caption` | string | No | Image caption. May be an empty string. |

### Not Found Response

Status:

```text
404 Not Found
```

Response body:

```json
{
  "detail": "Not found."
}
```

Common reasons:

- Slug does not exist.
- Project exists but `is_active=False`.

## Inquiries API

The inquiry endpoint accepts public contact form submissions.

It is write-only. `GET`, `PUT`, `PATCH`, and `DELETE` are not allowed.

### Create Inquiry

```http
POST /api/inquiries/
```

### Headers

```http
Content-Type: application/json
Accept: application/json
```

### Request Body

```json
{
  "name": "Aarav Sharma",
  "email": "aarav@example.com",
  "subject": "Partnership enquiry",
  "message": "We would like to discuss a possible collaboration."
}
```

### Request Fields

| Field | Type | Required | Max Length | Validation |
| --- | --- | --- | --- | --- |
| `name` | string | Yes | `150` | Must not be blank or whitespace only. |
| `email` | string | Yes | Email length handled by DRF/Django | Must be a valid email address. |
| `subject` | string | Yes | `200` | Must not be blank or whitespace only. |
| `message` | string | Yes | No explicit max length | Must not be blank or whitespace only. |

### Success Response

Status:

```text
201 Created
```

Response body:

```json
{
  "success": true,
  "message": "Your inquiry has been submitted successfully."
}
```

### Side Effects

When an inquiry is created:

1. A new `Inquiry` record is stored.
2. `is_read` defaults to `false`.
3. `is_archived` defaults to `false`.
4. The server attempts to send a notification email if `CONTACT_NOTIFICATION_EMAIL` is configured.

Email delivery failure does not change the success response of the API request.

### Validation Error Response

Status:

```text
400 Bad Request
```

Example response:

```json
{
  "name": [
    "Name is required."
  ],
  "email": [
    "Enter a valid email address."
  ],
  "subject": [
    "Subject is required."
  ],
  "message": [
    "Message is required."
  ]
}
```

### Method Not Allowed Response

Request:

```http
GET /api/inquiries/
```

Status:

```text
405 Method Not Allowed
```

Example response:

```json
{
  "detail": "Method \"GET\" not allowed."
}
```

## Statistics API

Statistics represent organization-wide public numbers shown on the website.

Only statistics with `is_active=True` are returned.

The endpoint returns a maximum of 4 statistics.

### List Statistics

```http
GET /api/statistics/
```

### Query Parameters

No query parameters are currently supported.

### Example Request

```http
GET /api/statistics/
```

### Success Response

Status:

```text
200 OK
```

Response body:

```json
{
  "count": 2,
  "next": null,
  "previous": null,
  "results": [
    {
      "title": "Projects Completed",
      "value": 48,
      "suffix": "+"
    },
    {
      "title": "Communities Reached",
      "value": 120,
      "suffix": "+"
    }
  ]
}
```

### Response Fields

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `count` | integer | No | Total number of matching statistics. |
| `next` | string | Yes | URL for the next page. |
| `previous` | string | Yes | URL for the previous page. |
| `results` | array | No | List of statistic objects. |
| `results[].title` | string | No | Statistic label. |
| `results[].value` | integer | No | Positive integer value. |
| `results[].suffix` | string | No | Optional display suffix. May be an empty string. |

## Clients API

Clients represent public client organizations that can be shown on the website and associated with projects.

Only clients with `is_active=True` are returned.

Clients are ordered by `display_order`, then `name`.

### List Clients

```http
GET /api/clients/
```

### Query Parameters

No query parameters are currently supported.

### Example Request

```http
GET /api/clients/
```

### Success Response

Status:

```text
200 OK
```

Response body:

```json
{
  "count": 2,
  "next": null,
  "previous": null,
  "results": [
    {
      "name": "Example Client",
      "logo": "/media/clients/logos/example.png",
      "website": "https://example.org",
      "description": "A short public description of the client."
    },
    {
      "name": "Another Client",
      "logo": "/media/clients/logos/another.png",
      "website": "",
      "description": ""
    }
  ]
}
```

### Response Fields

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `count` | integer | No | Total number of matching clients. |
| `next` | string | Yes | URL for the next page. |
| `previous` | string | Yes | URL for the previous page. |
| `results` | array | No | List of client objects. |
| `results[].name` | string | No | Client organization name. |
| `results[].logo` | string | No | Client logo URL/path. |
| `results[].website` | string | No | Client website URL. May be an empty string. |
| `results[].description` | string | No | Public client description. May be an empty string. |

## Updates API

Updates represent public announcements/news items that link out to more detail (for example a news post or an external page).

Only active updates are returned.

### List Updates

```http
GET /api/updates/
```

### Query Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `page` | integer | No | Page number. |

### Example Request

```http
GET /api/updates/
```

### Success Response

Status:

```text
200 OK
```

Response body:

```json
{
  "count": 1,
  "next": null,
  "previous": null,
  "results": [
    {
      "title": "New Partnership Announced",
      "image": "/media/updates/partnership.png",
      "link": "https://example.org/news/new-partnership-announced"
    }
  ]
}
```

### Response Fields

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `count` | integer | No | Total number of matching updates. |
| `next` | string | Yes | URL for the next page. |
| `previous` | string | Yes | URL for the previous page. |
| `results` | array | No | List of update objects. |
| `results[].title` | string | No | Update title. |
| `results[].image` | string | No | Update image URL/path. |
| `results[].link` | string | No | Destination URL for the update. |

## Blogs API

The Blogs API is mounted under the same unversioned `/api/` path as the other public endpoints. It was previously documented under `/api/v1/blogs/`; that versioned prefix has been removed.

Only published blog posts are returned.

### List Blogs

```http
GET /api/blogs/
```

### Query Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `category` | string | No | Filter by blog category. |
| `featured` | boolean | No | Filter by featured status. Use `true` or `false`. |
| `latest` | boolean | No | When `true`, biases results toward the most recently published posts. |
| `ordering` | string | No | Which field to use when ordering results. Prefix with `-` for descending order. |
| `page` | integer | No | Page number. |
| `page_size` | integer | No | Number of results to return per page. |
| `search` | string | No | A search term. |

### Example Requests

```http
GET /api/blogs/
```

```http
GET /api/blogs/?featured=true
```

```http
GET /api/blogs/?category=updates
```

```http
GET /api/blogs/?search=impact&page=2&page_size=6
```

### Success Response

Status:

```text
200 OK
```

Response body:

```json
{
  "count": 1,
  "next": null,
  "previous": null,
  "results": [
    {
      "title": "How We Measure Impact",
      "slug": "how-we-measure-impact",
      "excerpt": "A short summary of the blog post shown in listings.",
      "featured_image": "/media/blogs/covers/impact.png",
      "category": {
        "name": "Impact",
        "slug": "impact"
      },
      "author": "Aarav Sharma",
      "reading_time": 4,
      "published_at": "2026-05-01T09:00:00Z",
      "featured": true
    }
  ]
}
```

### Response Fields

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `count` | integer | No | Total number of matching blog posts. |
| `next` | string | Yes | URL for the next page. |
| `previous` | string | Yes | URL for the previous page. |
| `results` | array | No | List of blog list objects. |
| `results[].title` | string | No | Blog post title. |
| `results[].slug` | string | No | Unique blog post slug. |
| `results[].excerpt` | string | No | Short summary of the blog post. |
| `results[].featured_image` | string | No | Featured image URL/path. |
| `results[].category` | object | No | Blog category. Read-only. |
| `results[].category.name` | string | No | Category name. |
| `results[].category.slug` | string | No | Category slug. |
| `results[].author` | string | No | Author display name. Read-only. |
| `results[].reading_time` | integer | No | Estimated reading time in minutes. Read-only. |
| `results[].published_at` | string | Yes | Publish date/time in ISO 8601 format, or `null` if unpublished. |
| `results[].featured` | boolean | No | Whether the blog post is featured. |

### Retrieve Blog Detail

```http
GET /api/blogs/{slug}/
```

Returns one published blog post by slug.

### Path Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `slug` | string | Yes | Blog post slug. |

### Example Request

```http
GET /api/blogs/how-we-measure-impact/
```

### Success Response

Status:

```text
200 OK
```

Response body:

```json
{
  "title": "How We Measure Impact",
  "slug": "how-we-measure-impact",
  "excerpt": "A short summary of the blog post shown in listings.",
  "featured_image": "/media/blogs/covers/impact.png",
  "category": {
    "name": "Impact",
    "slug": "impact"
  },
  "author": "Aarav Sharma",
  "reading_time": 4,
  "published_at": "2026-05-01T09:00:00Z",
  "featured": true,
  "content": "Full HTML/markdown content of the blog post.",
  "meta_title": "How We Measure Impact | IKAAI INDIA",
  "meta_description": "Learn how IKAAI INDIA measures the impact of its programs.",
  "og_image": "/media/blogs/og/impact.png",
  "canonical_url": "https://example.org/blog/how-we-measure-impact",
  "related_blogs": [
    {
      "title": "Our 2025 Annual Report",
      "slug": "our-2025-annual-report",
      "excerpt": "A short summary of the blog post shown in listings.",
      "featured_image": "/media/blogs/covers/annual-report.png",
      "category": {
        "name": "Impact",
        "slug": "impact"
      },
      "author": "Aarav Sharma",
      "reading_time": 6,
      "published_at": "2026-04-01T09:00:00Z",
      "featured": false
    }
  ]
}
```

### Response Fields

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `title` | string | No | Blog post title. |
| `slug` | string | No | Unique blog post slug. |
| `excerpt` | string | No | Short summary of the blog post. |
| `featured_image` | string | No | Featured image URL/path. |
| `category` | object | No | Blog category. Read-only. |
| `author` | string | No | Author display name. Read-only. |
| `reading_time` | integer | No | Estimated reading time in minutes. Read-only. |
| `published_at` | string | Yes | Publish date/time in ISO 8601 format, or `null` if unpublished. |
| `featured` | boolean | No | Whether the blog post is featured. |
| `content` | string | No | Full blog post content. |
| `meta_title` | string | No | SEO title. May be an empty string. |
| `meta_description` | string | No | SEO description. May be an empty string. |
| `og_image` | string | Yes | Open Graph image URL/path, or `null` if not set. |
| `canonical_url` | string | No | Canonical URL for SEO. May be an empty string. |
| `related_blogs` | array | No | Read-only list of related blog posts, each shaped like a [Blog List object](#response-fields-1). |

> **Known schema-generation issue:** `drf-spectacular` currently can't infer the return type of `get_related_blogs()` (no type hint on the `SerializerMethodField`), so the generated `openapi.json` falls back to `type: string` for `related_blogs`. The actual response is an array of blog objects as shown above. Fix by adding a return type hint or an `@extend_schema_field` annotation to `get_related_blogs` in `serializers.py`.

### Not Found Response

Status:

```text
404 Not Found
```

Response body:

```json
{
  "detail": "Not found."
}
```

Common reasons:

- Slug does not exist.
- Blog post exists but is not published.

## Jobs API

The Jobs API is mounted under `/api/jobs/` and exposes public job openings and a public job-application submission endpoint.

Only active job openings are returned by the list and detail endpoints.

### List Jobs

```http
GET /api/jobs/jobs/
```

Returns a paginated list of active job openings.

### Query Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `page` | integer | No | Page number. |
| `page_size` | integer | No | Number of items per page. |
| `department` | string | No | Filter by department. |
| `location` | string | No | Filter by location. |
| `employment_type` | string | No | Filter by employment type. One of `Full Time`, `Part Time`, `Internship`, `Contract`, `Volunteer`. |
| `featured` | boolean | No | Filter by featured status. Use `true` or `false`. |
| `search` | string | No | Searches job title, location, and department. |
| `ordering` | string | No | Sort by `display_order` or `title` (default). Prefix with `-` for descending order. |

### Example Requests

```http
GET /api/jobs/jobs/
```

```http
GET /api/jobs/jobs/?department=Engineering
```

```http
GET /api/jobs/jobs/?employment_type=Full%20Time
```

```http
GET /api/jobs/jobs/?featured=true
```

```http
GET /api/jobs/jobs/?search=designer&page=2&page_size=6
```

### Success Response

Status:

```text
200 OK
```

Response body:

```json
{
  "count": 1,
  "next": null,
  "previous": null,
  "results": [
    {
      "title": "Frontend Engineer",
      "slug": "frontend-engineer",
      "department": "Engineering",
      "location": "Pune, India",
      "employment_type": "Full Time",
      "experience_level": "1–3 Years",
      "salary": "Competitive, based on experience",
      "description": "Short summary of the role shown in listings.",
      "application_deadline": "2026-08-31",
      "featured": true
    }
  ]
}
```

### Response Fields

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `count` | integer | No | Total number of matching jobs. |
| `next` | string | Yes | URL for the next page. |
| `previous` | string | Yes | URL for the previous page. |
| `results` | array | No | List of job objects. |
| `results[].title` | string | No | Job title. |
| `results[].slug` | string | No | Unique job slug. |
| `results[].department` | string | No | Department name. |
| `results[].location` | string | No | Job location. |
| `results[].employment_type` | string | No | One of `Full Time`, `Part Time`, `Internship`, `Contract`, `Volunteer`. |
| `results[].experience_level` | string | No | One of `Fresher`, `0–1 Years`, `1–3 Years`, `3–5 Years`, `5+ Years`. |
| `results[].salary` | string | No | Salary text. May be an empty string. |
| `results[].description` | string | No | Short job summary. |
| `results[].application_deadline` | string | Yes | Deadline date (`YYYY-MM-DD`), or `null` if not set. |
| `results[].featured` | boolean | No | Whether the job is featured. |

### Retrieve Job Detail

```http
GET /api/jobs/jobs/{slug}/
```

Returns one active job opening by slug.

### Path Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `slug` | string | Yes | Job slug. |

### Example Request

```http
GET /api/jobs/jobs/frontend-engineer/
```

### Success Response

Status:

```text
200 OK
```

Response body:

```json
{
  "title": "Frontend Engineer",
  "department": "Engineering",
  "location": "Pune, India",
  "employment_type": "Full Time",
  "experience_level": "1–3 Years",
  "salary": "Competitive, based on experience",
  "openings": 2,
  "description": "Full role description.",
  "responsibilities": "What the role involves day to day.",
  "requirements": "Skills and qualifications required.",
  "benefits": "Perks and benefits offered.",
  "application_deadline": "2026-08-31"
}
```

### Response Fields

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `title` | string | No | Job title. |
| `department` | string | No | Department name. |
| `location` | string | No | Job location. |
| `employment_type` | string | No | One of `Full Time`, `Part Time`, `Internship`, `Contract`, `Volunteer`. |
| `experience_level` | string | No | One of `Fresher`, `0–1 Years`, `1–3 Years`, `3–5 Years`, `5+ Years`. |
| `salary` | string | No | Salary text. May be an empty string. |
| `openings` | integer | No | Number of open positions. |
| `description` | string | No | Full role description. |
| `responsibilities` | string | No | Role responsibilities. |
| `requirements` | string | No | Role requirements. |
| `benefits` | string | No | Benefits offered. |
| `application_deadline` | string | Yes | Deadline date (`YYYY-MM-DD`), or `null` if not set. |

> Note: the detail response does not include `slug` or `featured` in the generated schema, since `JobDetailSerializer` doesn't declare them explicitly as read fields separate from the list serializer's `Meta`. Confirm against the live endpoint if the frontend needs those fields on the detail view too.

### Not Found Response

Status:

```text
404 Not Found
```

Response body:

```json
{
  "detail": "Not found."
}
```

Common reasons:

- Slug does not exist.
- Job exists but is not active.

### Submit Job Application

```http
POST /api/jobs/job-applications/
```

### Headers

```http
Content-Type: application/json
```

Requests without a JSON content type are rejected before validation.

### Request Body

The exact request fields are defined in `JobApplicationCreateSerializer`, which wasn't included with the files shared in this session, so the full field list can't be confirmed here. From `views.py`, the endpoint validates a `job` field (used to look up the job being applied to) and reports a specific error when the job is closed:

```json
{
  "job": [
    "Applications for this position are closed."
  ]
}
```

Share `serializers.py` for the `jobs` app and this section can be filled in completely (field names, types, required/optional, max lengths).

### Success Response

Status:

```text
201 Created
```

Response body:

```json
{
  "success": true,
  "message": "Application submitted successfully."
}
```

### Validation Error Responses

Status:

```text
400 Bad Request
```

Non-JSON request body:

```json
{
  "detail": "Request body must be JSON."
}
```

Closed job:

```json
{
  "detail": "Applications for this position are closed."
}
```

Other validation failures return `serializer.errors` directly, in the same shape as other endpoints in this document (field name mapped to a list of error messages).

### Known OpenAPI Generation Issue

`python manage.py spectacular` reports:

```text
Error [JobApplicationCreateAPIView]: unable to guess serializer.
```

This happens because `JobApplicationCreateAPIView` is a plain `APIView`, not a `GenericAPIView` with a `serializer_class`, so `drf-spectacular` can't introspect its request/response schema automatically. It's why the generated `openapi.json` shows this endpoint with no request body and a generic `200` "No response body" instead of the real `201` success response documented above. This is a schema-generation limitation, not a runtime bug — the endpoint behaves as documented above. To get it into the generated schema, either switch the view to `GenericAPIView` with `serializer_class = JobApplicationCreateSerializer`, or annotate it with `@extend_schema(request=JobApplicationCreateSerializer, responses=...)`.

## Browser Redirect Routes

These routes are public in the sense that a browser can open them, but they are not JSON APIs.

### Admin Login Page

```http
GET /admin/login/
```

Returns the Google-only admin login page.

### Google Login Start

```http
GET /accounts/google/login/
```

Redirects the browser to Google's OAuth consent/login flow.

### Google Callback

```http
GET /accounts/google/callback/
```

Handles Google's OAuth callback, validates the user, starts a Django session, and redirects to Django Admin.

Possible outcomes:

- Success: redirects to `/admin/`.
- Expired OAuth session: redirects to `/admin/login/` with an error message.
- Unprovisioned user: redirects to `/admin/login/` with an error message.
- Inactive user: redirects to `/admin/login/` with an error message.
- Non-staff user: redirects to `/admin/login/` with an error message.
- Google subject mismatch: redirects to `/admin/login/` with an error message.

## Data Limits and Business Rules

### Featured Projects

Only 4 active featured projects are allowed.

If an admin tries to save a fifth active featured project, validation fails.

### Organization Statistics

Only 4 active organization statistics are allowed.

The public endpoint also returns at most 4 active statistics ordered by `display_order`.

### Project Slugs

Project slugs are generated from titles when the project is first saved.

If another project already uses the same slug, the server appends a numeric suffix.

Example:

```text
annual-impact-report
annual-impact-report-2
annual-impact-report-3
```

After a slug is created, changing the title does not automatically change the slug.

## Frontend Integration Notes

Recommended frontend usage:

1. Use `/api/statistics/` for homepage/statistics sections.
2. Use `/api/clients/` for client logo or client listing sections.
3. Use `/api/projects/?featured=true` for featured project sections.
4. Use `/api/projects/` for project listing pages.
5. Use `/api/projects/{slug}/` for project detail pages.
6. Submit contact forms to `/api/inquiries/`.
7. Use `/api/updates/` for homepage news/updates sections.
8. Use `/api/blogs/?featured=true` for featured blog sections.
9. Use `/api/blogs/` for blog listing pages.
10. Use `/api/blogs/{slug}/` for blog detail pages.
11. Use `/api/jobs/jobs/?featured=true` for featured career sections.
12. Use `/api/jobs/jobs/` for the careers listing page.
13. Use `/api/jobs/jobs/{slug}/` for job detail pages.
14. Submit job applications to `/api/jobs/job-applications/`.

For image fields:

- Treat returned image strings as server-generated media URLs/paths.
- Prefix with the backend origin if the API returns a relative path.

Example:

```js
const imageUrl = new URL(project.cover_image, backendBaseUrl).toString();
```

## cURL Examples

### List Projects

```bash
curl "http://127.0.0.1:8000/api/projects/"
```

### List Featured Projects

```bash
curl "http://127.0.0.1:8000/api/projects/?featured=true"
```

### Get Project Detail

```bash
curl "http://127.0.0.1:8000/api/projects/annual-impact-report/"
```

### Submit Inquiry

```bash
curl -X POST "http://127.0.0.1:8000/api/inquiries/" \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "name": "Aarav Sharma",
    "email": "aarav@example.com",
    "subject": "Partnership enquiry",
    "message": "We would like to discuss a possible collaboration."
  }'
```

### List Statistics

```bash
curl "http://127.0.0.1:8000/api/statistics/"
```

### List Clients

```bash
curl "http://127.0.0.1:8000/api/clients/"
```

### List Updates

```bash
curl "http://127.0.0.1:8000/api/updates/"
```

### List Blogs

```bash
curl "http://127.0.0.1:8000/api/blogs/"
```

### Get Blog Detail

```bash
curl "http://127.0.0.1:8000/api/blogs/how-we-measure-impact/"
```

### List Jobs

```bash
curl "http://127.0.0.1:8000/api/jobs/jobs/"
```

### Get Job Detail

```bash
curl "http://127.0.0.1:8000/api/jobs/jobs/frontend-engineer/"
```

### Submit Job Application

```bash
curl -X POST "http://127.0.0.1:8000/api/jobs/job-applications/" \
  -H "Content-Type: application/json" \
  -d '{
    "job": "frontend-engineer"
  }'
```

> The example body above only shows the field confirmed from `views.py` (`job`). Add the remaining applicant fields once `serializers.py` for the `jobs` app is available.

## JavaScript Fetch Examples

### Fetch Projects

```js
async function getProjects() {
  const response = await fetch("http://127.0.0.1:8000/api/projects/");

  if (!response.ok) {
    throw new Error("Failed to fetch projects");
  }

  return response.json();
}
```

### Fetch Clients

```js
async function getClients() {
  const response = await fetch("http://127.0.0.1:8000/api/clients/");

  if (!response.ok) {
    throw new Error("Failed to fetch clients");
  }

  return response.json();
}
```

### Fetch Updates

```js
async function getUpdates() {
  const response = await fetch("http://127.0.0.1:8000/api/updates/");

  if (!response.ok) {
    throw new Error("Failed to fetch updates");
  }

  return response.json();
}
```

### Fetch Blogs

```js
async function getBlogs() {
  const response = await fetch("http://127.0.0.1:8000/api/blogs/");

  if (!response.ok) {
    throw new Error("Failed to fetch blogs");
  }

  return response.json();
}
```

### Fetch Blog Detail

```js
async function getBlog(slug) {
  const response = await fetch(`http://127.0.0.1:8000/api/blogs/${slug}/`);

  if (!response.ok) {
    throw new Error("Failed to fetch blog");
  }

  return response.json();
}
```

### Fetch Jobs

```js
async function getJobs() {
  const response = await fetch("http://127.0.0.1:8000/api/jobs/jobs/");

  if (!response.ok) {
    throw new Error("Failed to fetch jobs");
  }

  return response.json();
}
```

### Fetch Job Detail

```js
async function getJob(slug) {
  const response = await fetch(`http://127.0.0.1:8000/api/jobs/jobs/${slug}/`);

  if (!response.ok) {
    throw new Error("Failed to fetch job");
  }

  return response.json();
}
```

### Submit Job Application

```js
async function submitJobApplication(payload) {
  const response = await fetch("http://127.0.0.1:8000/api/jobs/job-applications/", {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify(payload)
  });

  const data = await response.json();

  if (!response.ok) {
    return {
      ok: false,
      errors: data
    };
  }

  return {
    ok: true,
    data
  };
}
```

### Submit Inquiry

```js
async function submitInquiry(formData) {
  const response = await fetch("http://127.0.0.1:8000/api/inquiries/", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "Accept": "application/json"
    },
    body: JSON.stringify({
      name: formData.name,
      email: formData.email,
      subject: formData.subject,
      message: formData.message
    })
  });

  const data = await response.json();

  if (!response.ok) {
    return {
      ok: false,
      errors: data
    };
  }

  return {
    ok: true,
    data
  };
}
```

## Public API Checklist for Future Endpoints

When adding a new public endpoint, document:

- Method and path.
- Authentication requirement.
- Request headers.
- Path parameters.
- Query parameters.
- Request JSON body.
- Success response JSON.
- Validation error response JSON.
- Method restrictions.
- Business rules.
- Side effects.
- Frontend integration notes.
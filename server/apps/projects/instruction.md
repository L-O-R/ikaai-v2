# Projects Module Instruction

Implement project management for IKAAI INDIA CMS using the shared CMS architecture.

Rules:

- Use `BaseModel`
- Keep queries in `selectors.py`
- Keep business rules in `services.py`
- Use `BaseAdmin`
- Expose read-only public APIs only
- Manage statistics and gallery images as inlines in Django Admin

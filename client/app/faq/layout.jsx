import React from 'react';
import { faqData } from '@/lib/data/faqData';

export const metadata = {
    title: 'Frequently Asked Questions | IKAAI India',
    description: 'Find answers regarding IKAAI India\'s research services, project implementations, M&E empirical studies, institutional partnerships, and field operations.',
};

export default function FAQLayout({ children }) {
    return children;
}
